// Check if localStorage is available
if (typeof localStorage !== 'undefined') {
    console.log('localStorage is available in this browser.');
  } else {
    console.log('localStorage is not available in this browser.');
  }
  
  // Check if sessionStorage is available
  if (typeof sessionStorage !== 'undefined') {
    console.log('sessionStorage is available in this browser.');
  } else {
    console.log('sessionStorage is not available in this browser.');
  }